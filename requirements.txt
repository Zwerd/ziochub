Flask==3.0.0
Flask-Login==0.6.3
Flask-SQLAlchemy==3.1.1
Werkzeug==3.0.1
geoip2>=4.7.0
maxminddb>=2.3.0
ldap3>=2.9
pymisp>=2.4.187
# YARA: in-app syntax check (Write tab) uses yara.compile. Install: pip install -r requirements.txt
# Offline: include yara_python-*.whl for your OS/Python in packages/ (see OFFLINE.md). From sdist: need libyara-dev + build tools.
yara-python==4.5.0
# DXL / TIE (optional: for ePO TIE hash reputation updates)
dxlclient>=5.0.0
dxltieclient>=0.3.0
# Google SecOps / Chronicle: Integrations “Test connection” (OAuth refresh + optional API base probe)
google-auth>=2.23.0
requests>=2.28.0
